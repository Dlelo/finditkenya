extends ../layout

block content
  div.container-fluid
  #home.parallax-content-list.baner-content-listing-biz
    each product in products
      .col-md-3
        h4
          =product.name
        p
          =product.description
        .alert.alert-danger
          =product.price
